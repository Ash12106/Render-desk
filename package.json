{
  "name": "support-ops-desk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "seed": "npm run seed --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "preview": "npm run preview --workspace=client",
    "clean": "npm run clean --workspace=client && npm run clean --workspace=server",
    "lint": "npm run lint --workspace=client && npm run lint --workspace=server",
    "lint:eslint": "npm run lint:eslint --workspace=client && npm run lint:eslint --workspace=server",
    "format": "prettier --write \"client/src/**/*.{ts,tsx}\" \"server/src/**/*.ts\" \"client/*.ts\" \"server/*.ts\" \"*.json\"",
    "format:check": "prettier --check \"client/src/**/*.{ts,tsx}\" \"server/src/**/*.ts\" \"client/*.ts\" \"server/*.ts\" \"*.json\"",
    "test": "npm run test --workspace=client && npm run test --workspace=server"
  }
}
